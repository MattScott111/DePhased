#ifndef VECTOR3_H
#define VECTOR3_H

namespace DePhased {
    // If you define your Vector3 to exactly match Raylib's layout,
    // you can cast them directly or alias them.
    struct Vector3 {
        float x, y, z;
    };
}
#endif
