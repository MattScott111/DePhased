# Weapon & Defensive Protocol

- **Signature-Based Stealth**: Weapons emit `AcousticIntensity`. The AI detects the *emissions*, not the entity itself.
- **Resource Feedback**: Impact absorption from shields is refracted back into `EnergyPool`.
- **Mobility Trade-offs**: Heavy weapons apply multipliers to `Velocity` arrays, forcing a choice between power and traversal speed.
